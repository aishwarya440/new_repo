Verify Opportunity is NOT moved to Abandoned Queue when Product is selected
Verify Opportunity is NOT marked as “Closed Lost – Abandoned” for valid DDD submissions
Verify Opportunity is NOT moved to DDD Queue when no Product is selected
Verify Opportunity is NOT moved to DDD Drop Out Queue if application is not cancelled
Verify Opportunity does NOT appear in multiple queues simultaneously (Abandoned / DDD / Drop Out)
Verify non-DDD opportunities are NOT impacted by Product selection / Abandoned queue logic
Verify duplicate queue movement does NOT occur when Opportunity update runs multiple times
Verify invalid/missing Opportunity data does NOT incorrectly trigger Abandoned status update
Verify cancelled application without prior Product selection does NOT move to DDD Drop Out Queue
Verify manual Opportunity updates do NOT bypass queue movement rules
